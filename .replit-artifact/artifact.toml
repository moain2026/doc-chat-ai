kind = "web"
previewPath = "/"
title = "AI Document Chat"
version = "1.0.0"
id = "artifacts/doc-chat"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23495

[services.development]
run = "pnpm --filter @workspace/doc-chat run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/doc-chat", "run", "build" ]
publicDir = "artifacts/doc-chat/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23495"
BASE_PATH = "/"
